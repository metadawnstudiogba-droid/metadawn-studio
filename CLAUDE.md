# Claude Code instructions

Follow [AGENTS.md](AGENTS.md) for installation, security, testing, and provider-adapter constraints.

For a local installation, use Node.js 24 LTS, run `npm ci`, `npm run setup:local`, and `npm run start:local`. Do not use Docker or ask the user to paste provider or R2 credentials into the terminal; those credentials are entered in the browser UI after startup.
